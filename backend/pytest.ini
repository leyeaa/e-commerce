[pytest]
DJANGO_SETTINGS_MODULE = backend.settings_test
python_files = test_*.py
testpaths = tests
addopts = -ra --strict-markers
markers =
    known_defect: executable evidence for a defect recorded before the experiment
    manual: human-authored deterministic API test
